# Session 6830d7a1 — 2026-08-30

## Status: PAUSED, not complete.

## Worktree / Branch
- Worktree: /home/toufic/Source/article-pipeline
- Branch: main (latest commit: `9d2b714` — Merge PR #2)

## What's uncommitted right now

```
git status --short
 M docker-compose.yml
?? writer_examples.txt
```

- `docker-compose.yml` — added bind-mount for `writer_examples.txt` (line: `./writer_examples.txt:/app/writer_examples.txt`). **Not committed** because the user may still want to add more examples or adjust the file before we finalize.
- `writer_examples.txt` — user-created file with 4 real LinkedIn post examples for the `WRITER_EXAMPLES` few-shot feature. **Untracked.** Contains the user's real writing — do NOT commit this without explicit user permission (it's personal content, not code).

Both are non-blocking — the Docker container is already running with both mounted. They just aren't in git.

## What was done this session

### 1. UI fixes (committed as `9a69ff1`, merged via PR #1)
- **Thread ID drop-down**: input changed to a datalist backed by `GET /api/threads` (returns 10 most recent threads from `costs.db` usage table). Users can still type a new thread ID. New `cost_tracker.list_recent_threads(limit=10)`.
- **Model defaults**: Ollama default changed from `llama3.1` to `deepseek-v4-flash:cloud` in `graph.py` fallback + UI placeholders.
- **Live State JSON collapsed**: wrapped in `<details>` block (same pattern as 3b Raw Articles), collapsed by default.

### 2. Paywall/subscription exclusion (committed as `9a69ff1`, merged via PR #1)
Evolved through three iterations based on user feedback:
- **Iteration 1 (domain blocklist only)**: Added `PAYWALLED_DOMAINS` env var with a large blocklist including medium.com, substack.com. User rejected this — "medium and substack are a great source of technical articles!"
- **Iteration 2 (fetch probe only)**: Replaced with `_is_paywalled_article()` fetch probe that fetches each article body and detects paywall markers. User wanted to re-evaluate options.
- **Iteration 3 (hybrid — final)**: Three env-configurable layers:
  1. **Domain blocklist** (`PAYWALLED_DOMAINS`): small list of genuinely all-paywall, non-technical domains only (`theinformation.com,thelogic.co`). Never includes mixed platforms like medium.com or substack.com.
  2. **Content markers** (`PAYWALL_MARKERS`): 16 phrases scanned in title+summary at RSS level (no fetch). Catches paid Substack/Medium posts and paywall teasers.
  3. **Fetch probe** (`PAYWALL_PROBE=false` by default, `PAYWALL_PROBE_MAX=40`): opt-in, fetches article bodies and detects paywalls in HTML. Most accurate but slow.
  
  Each mechanism has a distinct drop reason in the scout audit: `paywall_domain`, `paywall_marker`, `paywalled`.

### 3. Writer prompt improvement (committed as `6830d7a`, merged via PR #2)
Five changes to reduce AI-sounding output:
- **A — Rewritten prompt structure** (`graph.py:1399`): Collapsed labelled `VOICE:`/`STRUCTURE:`/`EXAMPLE OUTPUT:` blocks into a flowing editorial brief. Removed the synthetic persona examples from the prompt (they made every post sound like a variation of one example). Added `_ANTI_AI_TELLS` (graph.py:86) — hard constraints banning "delve", "navigate", "landscape", "leverage", "the promise is X, the reality is Y", em dashes, symmetric bullets, hedging, neat summary sentences.
- **B — Few-shot from user's writing** (`settings.py:135`, `graph.py:1431`): `WRITER_EXAMPLES` env var. Set to inline text (examples separated by `---` lines) or `file:/path/to/file`. When set, examples injected as "Here's how I actually write — match this voice." Empty by default. User created `writer_examples.txt` with 4 real posts, mounted in Docker, and set `.env` to `WRITER_EXAMPLES=file:/app/writer_examples.txt`.
- **C — Free-text feedback on draft** (`ui.py:599`): Custom feedback textarea + "Refine with Feedback" button next to Quick Refine. Wires through existing `/api/refine` with anti-AI-tells applied.
- **D — Learn from manual edits** (`ui.py:2228`, `ui.py:1464`): On publish, `/api/learn-from-edit` compares edited draft against original AI draft (from `draft_versions` where source="author"), uses LLM to extract one concise style rule, shows a toast offering one-click save (source: `learned_from_edit`).
- **E — Article body fetch** (`graph.py:1497`): `author_node` now fetches the full article body (up to 200KB, fail-open) via `_http_fetch_text()` and passes it to the writer instead of just the 1600-char RSS summary. Less fabrication, more grounded output.

## Decisions made and why

1. **Paywall approach went through 3 iterations** — user explicitly rejected domain-blocking medium.com/substack.com (too blunt), then chose "fetch probe only" (B), then chose "hybrid" (C) after being re-presented with all options. The hybrid keeps all technical sources while catching paywalled content. The fetch probe defaults OFF to keep the synchronous scout fast.

2. **Writer prompt: removed synthetic persona examples** — the old prompt injected a single example about agentic workflows (the exact topic domain), so every post pattern-matched that example's vocabulary and rhythm. Replaced with user's own writing via `WRITER_EXAMPLES`. If no examples are configured, the prompt still works (just without few-shot voice matching).

3. **`WRITER_EXAMPLES` uses `file:` prefix for Docker** — docker compose's `.env` parser can't handle multi-line values with `---` separators. The user's examples live in a separate file (`writer_examples.txt`) bind-mounted into the container. `.env` points to `file:/app/writer_examples.txt`.

4. **`docker-compose.yml` and `writer_examples.txt` left uncommitted** — the user just created the examples file and may still want to adjust it. `docker-compose.yml` change is trivial (one volume mount line) but left uncommitted alongside the examples file since they're related.

5. **All work was committed and merged to main** — both PRs (#1 and #2) were merged. The only uncommitted files are the two from the last step (docker-compose.yml mount + writer_examples.txt).

## Dead ends ruled out

- **Domain-blocking medium.com/substack.com** — user explicitly rejected. These platforms have huge amounts of free technical content. Don't re-add them to `PAYWALLED_DOMAINS`.
- **Multi-line `WRITER_EXAMPLES` in `.env`** — docker compose's `.env` parser fails with `key cannot contain a space` on multi-line values. Must use `file:` prefix for Docker. python-dotenv handles multi-line fine locally, but Docker is the deployment target.
- **Injecting the old synthetic persona examples** — made output sound MORE AI, not less. The model pattern-matched the example's structure. Removed in favor of user's own examples.

## Open questions waiting on the user

- **None explicitly blocking.** The user's last interaction was asking about whether changing the writer model mid-stream would affect refine (answer: no, model is locked in checkpoint state at run start). No follow-up was requested.

## Session-Id trailer gap

No commits in this session carry a `Session-Id` trailer — the commits were made before the session-pause protocol was initiated. Commits: `9a69ff1` (PR #1), `6830d7a` (PR #2), `c835f87` (merge #1), `9d2b714` (merge #2). Not rewriting history to fix this.

## Next concrete action

Commit the two uncommitted files (`docker-compose.yml` and `writer_examples.txt`) — but only after confirming with the user that `writer_examples.txt` is finalized (it contains their personal writing). The `docker-compose.yml` change is safe to commit independently if the user prefers.

After that, the user may want to:
- Test the new writer prompt with their examples against a real article run
- Adjust the `PAYWALL_MARKERS` or `PAYWALLED_DOMAINS` lists based on live results
- Add more style rules via the UI's Style Rules panel
- Continue with any other improvements

No work is broken or incomplete — the container is running with all features active.